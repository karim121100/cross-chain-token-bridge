# cross-chain-token-bridge
A bidirectional bridge protocol. It allows users to lock ERC-20 tokens on a "Source Chain" to mint equivalent Wrapped Tokens on a "Destination Chain." Includes the smart contracts and the off-chain event listener (validator) node.
